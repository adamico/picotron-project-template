systems.animateActors = World.system({Actor, Animation}, function(entity)
	-- local sprite = entity[Sprite]
	-- local sprites = animation.sprites
	-- local sprite_index = flr((t()*6)%4+1)
	-- sprite.number = sprites[sprite_index]
end)